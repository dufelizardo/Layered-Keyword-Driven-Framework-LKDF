# Robot Framework generated reports (regenerated on every run, never source)
log.html
output.xml
report.html

# Node / Playwright (regenerated on every run/install, never source)
node_modules/
playwright-report/
test-results/
